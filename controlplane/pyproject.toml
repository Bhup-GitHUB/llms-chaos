[project]
name = "llms-chaos-controlplane"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["ray[serve]>=2.40", "fastapi>=0.115", "uvicorn>=0.32", "httpx>=0.27"]

[project.optional-dependencies]
mlx = ["mlx-lm>=0.21"]
